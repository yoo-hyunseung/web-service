{{>layout/header}}{{! >는 현재 머스테치 파일 (index.mustache)을 기준으로 다른 파일을 가져옴니다.}}

    <h1>스프링 부트로 시작하는 웹 서비스 Ver.2</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            </div>
        </div>
        <br>
        <! -- 목록 출력 영영 -->
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>게시글 번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
            </thead>
            <tbody id="tbody">
            {{#posts}} {{! posts 라는 List 를 순회합니다. java for 문과 비슷한 역할}}
                <tr>
                    <td>{{id}}</td> {{! {{}}   {{!List에서 뽑아낸 객체의 필드를 사용한다}}
                    <td><a href="/posts/update/{{id}}">{{title}}</a> </td>
                    <td>{{author}}</td>
                    <td>{{modifiedDate}}</td>
                </tr>
            {{/posts}}
            </tbody>
        </table>
    </div>

{{>layout/footer}}